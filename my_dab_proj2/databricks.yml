# This is a Databricks asset bundle definition for my_dab_proj2.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: my_dab_proj2
  uuid: 06700714-26a0-4b53-8ef8-27b8a2262206

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-aa7014a2-6101.cloud.databricks.com/
    variables:
      catalog: dab_proj_cat
      schema: dab_proj_schema_dev
  prod:
    mode: production
    workspace:
      host: https://dbc-aa7014a2-6101.cloud.databricks.com/
      # We explicitly deploy to /Workspace/Users/bhargavivenu15@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/bhargavivenu15@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dab_proj_cat
      schema: dab_proj_schema_prod
    permissions:
      - user_name: bhargavivenu15@gmail.com
        level: CAN_MANAGE
